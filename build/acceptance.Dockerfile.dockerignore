# BuildKit prefers <dockerfile>.dockerignore over the context-root .dockerignore, and this
# build needs that: the root file is upstream-owned (filter `keep`) and excludes `.*`, which
# would strip .mvn/community-maven.settings.xml and the .spi descriptor the image carries.
.git/
**/target/
**/Dockerfile
