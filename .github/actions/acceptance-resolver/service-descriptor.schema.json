{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://azure.github.io/osdu-spi/schemas/service-descriptor-v3.schema.json",
  "title": "SPI service descriptor (schema v3)",
  "description": "Fork-owned acceptance contract at .spi/service.yaml. Declares what a service's acceptance suite needs, symbolically: bindings from a closed source vocabulary resolved per run against the stack's facts envelope (spi info --json, apiVersion spi.osdu.dev/v1), Key Vault secret references (names, never values), required loads and entitlement groups, sibling dependencies, and a timeout. The resolver engine (.github/actions/acceptance-resolver) is the authority on enforcement; it additionally rejects reserved environment variable names (exact: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID, AZURE_FEDERATED_TOKEN_FILE, and common process names such as PATH, HOME, LD_PRELOAD, MAVEN_OPTS; prefixes: ACTIONS_, GITHUB_, RUNNER_, SPI_STACK_) and any key outside this schema, exiting 2 with the offending key named.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "service", "tests"],
  "properties": {
    "schemaVersion": {
      "const": 3,
      "description": "Contract version. Unknown versions are a hard failure; the vocabulary grows only by a template contract change."
    },
    "service": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "archetype"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]{0,62}$",
          "description": "Service slug, e.g. partition."
        },
        "archetype": {
          "const": "java-maven-azure",
          "description": "Build/test archetype. Deliberately closed; new archetypes arrive by contract change."
        },
        "description": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "tests": {
      "type": "object",
      "required": ["acceptance"],
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]{0,31}$"
      },
      "additionalProperties": {
        "$ref": "#/$defs/suite"
      },
      "properties": {
        "acceptance": {
          "$ref": "#/$defs/suite"
        }
      },
      "description": "Named suites of one shape. acceptance is required and is the upstream <svc>-acceptance-test by default; a fork adds others, for example integration for its provider suite. The acceptance image bakes every declared suite path."
    }
  },
  "$defs": {
    "binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source"],
      "properties": {
        "source": {
          "type": "string",
          "pattern": "^(gateway|partition|openid|tenant|legalTag|domain|static|template|user|token|memberToken|noAccessToken|keyvault:[A-Za-z0-9][A-Za-z0-9-]{0,126})$",
          "description": "Closed source vocabulary. Facts: gateway (base_url), partition (primary partition name), openid (OIDC v2.0 issuer URL, published explicitly by the stack), tenant (azure.tenant_id), legalTag (primary partition's legal_tag), domain (the entitlements domain the stack deployed, published as entitlements_domain). keyvault:<name> reads that secret from the stack's vault. static carries the declared value; template renders ${NAME} references to other (non-template, non-secret) bindings after everything else resolves; user must be supplied by the caller's environment; token is the bearer the caller minted for this run, supplied as RESOLVER_TOKEN; memberToken is the bearer minted for the stack's member identity, a plain user with no admin rights, supplied as RESOLVER_MEMBER_TOKEN; noAccessToken is the bearer minted for the stack's no-access identity, supplied as RESOLVER_NO_ACCESS_TOKEN. Anything else is a hard failure naming the key."
        },
        "suffix": {
          "type": "string",
          "maxLength": 200,
          "pattern": "^[A-Za-z0-9._~:/@+-]+$",
          "description": "Appended to a fact-sourced value (gateway values have their trailing slash stripped first). Fact sources only."
        },
        "value": {
          "type": "string",
          "maxLength": 240,
          "description": "Required for static and template sources; invalid elsewhere."
        },
        "default": {
          "type": "string",
          "description": "Fallback when the fact or caller environment does not supply an answer. Invalid for keyvault, token, memberToken, and noAccessToken sources (a default secret would put a secret value in the repository) and for static/template."
        }
      }
    },
    "suite": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "path"],
      "properties": {
        "type": {
          "const": "maven"
        },
        "path": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._/-]{0,119}$",
          "description": "Repository-relative working directory of the suite, e.g. the upstream <svc>-acceptance-test module."
        },
        "mavenArguments": {
          "type": "array",
          "minItems": 1,
          "default": ["verify"],
          "items": {
            "type": "string",
            "maxLength": 240,
            "pattern": "^[^\\s\\u0000-\\u001f\\u007f]+$"
          },
          "description": "Individual Maven argv tokens. Consumers pass the decoded array directly to Maven and never evaluate it as a shell string."
        },
        "bindings": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[A-Za-z_][A-Za-z0-9_]{0,127}$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/binding"
          },
          "description": "Environment variables the suite reads, each bound to a symbol from the closed source vocabulary -- never to an environment value. Resolution precedence per variable: explicit process environment (verbatim) -> source resolution -> declared default. template sources render last."
        },
        "keyVaultBindings": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[A-Za-z_][A-Za-z0-9_]{0,127}$"
          },
          "additionalProperties": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9-]{0,126}$"
          },
          "description": "Environment variable to Key Vault secret NAME, resolved from the stack's vault at run time. Secret values never appear in the descriptor."
        },
        "requires": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "loads": {
              "type": "array",
              "default": [],
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9-]{0,62}$"
              },
              "description": "Named data loads the environment must have seeded (checked against published load facts by the deploy gate)."
            },
            "groups": {
              "type": "array",
              "default": [],
              "items": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9._@-]{0,254}$"
              },
              "description": "Entitlement groups the test caller must hold."
            }
          }
        },
        "dependencies": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{0,62}$"
          },
          "description": "Sibling services this suite calls; a pinned dependency blocks the deploy gate with a typed reason."
        },
        "timeoutMinutes": {
          "type": "integer",
          "minimum": 1,
          "maximum": 180,
          "default": 25
        }
      }
    }
  }
}
